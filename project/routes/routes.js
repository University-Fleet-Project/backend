const express=require('express');const {pool}=require('../config/db');const {ok,fail,userId}=require('../utils');const {requireAuth}=require('../middleware/auth');const router=express.Router();
function hav(a,b,c,d){const R=6371;const x=(c-a)*Math.PI/180,y=(d-b)*Math.PI/180;const h=Math.sin(x/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(y/2)**2;return 2*R*Math.asin(Math.sqrt(h));}
router.post('/estimate',requireAuth,async(req,res)=>{const {origin,destination,reservationId}=req.body;if(origin?.latitude==null||origin?.longitude==null||destination?.latitude==null||destination?.longitude==null)return fail(res,400,'VALIDATION_ERROR','Origin and destination coordinates are required.');const distance=Number((hav(origin.latitude,origin.longitude,destination.latitude,destination.longitude)*1.2).toFixed(2));const duration=Math.round(distance/40*60);try{const r=await pool.query(`INSERT INTO route_estimates(reservation_id,origin,destination,distance_km,duration_minutes,provider,snapshot) VALUES($1,$2,$3,$4,$5,'mock',$6) RETURNING *`,[reservationId||null,JSON.stringify(origin),JSON.stringify(destination),distance,duration,JSON.stringify({method:'haversine*1.2'} )]);return ok(res,{distanceKm:distance,durationMinutes:duration,provider:'mock',timestamp:r.rows[0].created_at,route:{polyline:null}},'Route estimated');}catch(e){return fail(res,500,'ROUTE_ERROR','Unable to save route estimate.');}});
module.exports=router;
